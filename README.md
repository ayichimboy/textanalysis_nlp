# topic_modelling

When building a RAG system, it is important that the semantic and contextual meanings are maintained
RAG which means Retrieval Augmented Generation

Retrival:
retrive the files after you load file
the files can be text, pdf or others
using text loaders are for the text to be loaded

After loading the files, spli the files into chunks.
chunk sizes allows for sentences to be chunked or sized correctly for creating embeddings
